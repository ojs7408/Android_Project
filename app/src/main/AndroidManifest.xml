<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sliding_test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_hacheol"
        android:label="Hacheol2"
        android:roundIcon="@mipmap/ic_launcher_hacheol_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Statistics"></activity>
        <activity android:name=".Popupmain" android:theme="@android:style/Theme.Dialog" />
    </application>

    <uses-permission android:name="android.permission.INTERNET"/>

</manifest>